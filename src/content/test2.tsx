export const config = {
  component: true,
  shadow: false,
}

export const style = `
  .test-data {
    background: #000;
  }
`

const PlasmoOverlay = () => {
  return <div>ddd</div>
}

const a = '456'
console.log(a);
export default PlasmoOverlay